/**
 * Loader component
 * @returns a styled loading component for when the page is taking awhile to load
 * @author ShaAnder
 */
export function Loader() {
  return <p className="loader">Loading...</p>;
}
