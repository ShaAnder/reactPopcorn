/**
 * Main Component
 * @param {*} params -> children
 * @returns main component that styles the page, mainly a visual rendering componetn
 * @author ShaAnder
 */
export function Main({ children }) {
  return <main className="main">{children}</main>;
}
